<!DOCTYPE html>
<html>
<head>
  <title>Anki</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="navbar navbar-inverse">
  <div class="navbar-inner">
    <ul class="nav">
      <li><%= link_to 'Домой', root_path %></li>
      <% if user_signed_in? %>
        <li><%= link_to 'Создать новую карточку', new_card_path %></li>
        <li><%= link_to 'Список карточек', cards_path %></li>
        <li><%= link_to 'Редактировать профиль', edit_user_registration_path, :class => 'navbar-link' %></li>
        <li><%= link_to "Выйти", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %></li>
      <% else %>
        <li><%= link_to "Зарегистрироваться", new_user_registration_path, :class => 'navbar-link'  %></li>
        <li><%= link_to "Войти", new_user_session_path, :class => 'navbar-link'  %></li>
      <% end %>
    </ul>
    <% if user_signed_in? %>
      <div class='accaunt_name'>Вы зашли как: <span id="user"><%= current_user.login %></span></div>
    <% end %>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="span12">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </div>
</div>

</body>
</html>
